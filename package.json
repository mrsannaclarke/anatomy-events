{
  "name": "events-app-2-0",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "lint": "eslint src functions shared vite.config.js",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "deploy": "npm run build && wrangler pages deploy ./dist --project-name anatomy-events --branch main",
    "deploy:check": "npm run build && wrangler pages deploy ./dist --project-name anatomy-events --branch preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "jose": "^6.2.8",
    "lucide-react": "^1.31.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "typescript": "^5.9.2",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.1",
    "globals": "^17.11.0",
    "wrangler": "^4.120.0"
  }
}
